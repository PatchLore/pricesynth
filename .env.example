# Shadow Pricing Auditor — environment template
# Copy to .env and fill in values.

# Auth (required for free trial + login)
# Use a long random string: openssl rand -hex 32
JWT_SECRET=your-secret-at-least-32-chars

# Stripe (required for paid audits)
# Get test keys: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Optional: Publishable key for client-side (e.g. Stripe.js)
# STRIPE_PUBLISHABLE_KEY=pk_test_...

# Optional: Use mock LLM (no Ollama)
# USE_MOCK_LLM=0

# Optional: Test mode (simulate agents, 1s each)
# TEST_MODE=0

# Optional: Reddit scraping (PRAW)
# REDDIT_CLIENT_ID=...
# REDDIT_CLIENT_SECRET=...

# Optional: Better scraping
# SCRAPINGBEE_API_KEY=...
